Plan approved with two design corrections and one sequencing constraint.
Produce the diff, show it, stop. No commit, no push.

CORRECTION 1 — attach must happen contextually, not as a separate step.
As scoped, a crew member who takes photos the normal way and taps done
has zero attached photos and gets blocked with no explanation. That
reads as a broken feature.

Extend the upload chain the way upload-checklist-photos.ts already
does: photos captured FROM a task carry the taskId through
sign → S3 PUT → finalize media → attach, so they attach on finalize
with no extra user action. Then add an "attach existing project photos"
picker as the deliberate path for photos already in the project.

Both paths go through POST /api/tasks/:id/photos. Report how the
checklist flow threads its item id so mobile can mirror it exactly.

CORRECTION 2 — strip, don't reject, requiredPhotoCount from non-admin
patches. A standard user PATCHing a full task object to change the
title must not get a 422 for a field they didn't touch. Silently ignore
the field for non-admins; existing value is preserved.

SEQUENCING — do NOT expose the requirement input to customers in this
diff. required_photo_count DEFAULT 0 keeps every existing client safe,
but installed mobile builds have never seen PHOTOS_REQUIRED and will
surface it as a generic failure. Put the admin requirement input behind
a flag I can enable on my own account only, so I can dogfood it before
mobile ships support.

MIGRATION — use the scripts/migrations/ template
(migrate_session22_timesheets.ts): db.transaction(), env guard throwing
unless the URL is rds.amazonaws.com or neon.tech, ALLOW_PROD_MIGRATION=yes
required for RDS, IF NOT EXISTS on every DDL, entry-point guard. Both
changes are additive with defaults. Write it, do not run it against prod.

ALSO CONFIRM IN THE DIFF:
- New endpoints scope through projects.accountId, matching the existing
  media access pattern — not just task-belongs-to-project.
- Attach rejects media whose projectId differs from the task's project.
- Cascade behavior: if an attached photo is deleted, the task_photos row
  goes and a completed task falls below its requirement. It must NOT
  reopen — confirm nothing recomputes status.
- The 422 body is machine-readable: { code, required, attached }.

DELIVERABLE: the migration script, the diff file by file, and the exact
API contract for mobile (endpoints, payloads, the 422 shape, and how
taskId threads through the upload chain). I'm handing that contract to
the mobile agent.
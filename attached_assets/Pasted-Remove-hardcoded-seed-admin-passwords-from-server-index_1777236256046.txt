Remove hardcoded seed admin passwords from server/index.ts.
Do NOT push to git. Show me the diff and wait for approval.

# Context

server/index.ts has hardcoded seed admin passwords (Georgia#22, Roswell#2018) 
that get bootstrap-applied on first run. These are in git history forever. 
Anyone with a checkout sees them.

We're moving to env-var-based config. After this prompt:
- Hardcoded passwords removed from source
- Seed admin emails (grayson@field-view.com, support@field-view.com or 
  whatever they are) still hardcoded in source — those aren't secrets
- If env vars are missing, skip the seed admin creation entirely with a 
  warning log. Do NOT fall back to a default hardcoded password — that 
  defeats the purpose.
- Passwords on the existing user records will be rotated separately 
  (manual step after this lands)

# Task

## 1. Find the seed admin password references

Search server/index.ts for the password values "Georgia#22" and 
"Roswell#2018", or the function that sets them. Cite file:line for each.

The function is likely bootstrapAdminAndOrphans or similar.

## 2. Read the existing logic

Show me the function. I want to see:
- What emails are bootstrapped
- What happens if the user already exists (skip? update?)
- Whether the function fires on every boot or only conditionally

## 3. Modify the function

Replace the hardcoded password strings with reads from env vars:
- SEED_ADMIN_PASSWORD_PRIMARY (was "Georgia#22")
- SEED_ADMIN_PASSWORD_SECONDARY (was "Roswell#2018")

If either env var is missing, skip that admin's seed creation and log:
  console.warn("[seed-admin] Skipping <email> — SEED_ADMIN_PASSWORD_<X> not set");

Do NOT fall back to a default. Missing env var = skip.

If both env vars are set, behavior is identical to today (creates the 
admin if not exists, with the new password).

## 4. Update .env.example

Document the new env vars:

  # Seed admin passwords — set on first deploy to bootstrap admin accounts
  # If unset, seed admins won't be created. Required for fresh deployments only.
  SEED_ADMIN_PASSWORD_PRIMARY=REPLACE_WITH_STRONG_PASSWORD
  SEED_ADMIN_PASSWORD_SECONDARY=REPLACE_WITH_STRONG_PASSWORD

Use sanitized placeholders so GitHub secret scanner doesn't choke.

## 5. What NOT to change

- The seed admin emails (those aren't secrets)
- The bootstrap logic flow (still runs on boot, still skips if user 
  exists)
- The accountId / role assignment for seed admins
- Any other code in server/index.ts
- The schema

## 6. Note

After this lands, the production app would FAIL to bootstrap admins 
on a fresh deploy unless SEED_ADMIN_PASSWORD_PRIMARY and 
SEED_ADMIN_PASSWORD_SECONDARY are set in Vercel env. Since the admins 
ALREADY EXIST in production, this won't break anything today — the 
function just no-ops on existing users. But for any future fresh 
deploy (e.g., a staging environment from scratch), the env vars 
must be set.

I'll separately rotate the actual passwords on the existing user 
records via a password reset flow after this lands. The old values 
(Georgia#22, Roswell#2018) currently work via hash lookup — they 
won't stop working until we explicitly change the password on those 
user records.

# Output

1. Diff for server/index.ts
2. Diff for .env.example
3. TS error count (target: 33)
4. Confirm: no other code changes, no git operations
5. Confirm what happens on fresh boot when env vars are missing 
   (should be: log warning + skip, no errors thrown)

Wait for me to review.
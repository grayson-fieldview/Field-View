PROMPT — Implement Meta Conversions API (CAPI) Server-Side Events
Context: Investigation complete. You will now implement Meta CAPI. Follow this spec exactly. When done, show the full git diff and STOP. Do not commit, do not push.
Part 1 — New module server/lib/metaCapi.ts
Model the structure on server/lib/ghl.ts but with these differences:

Env vars read at call time (not module load): META_PIXEL_ID, META_CAPI_ACCESS_TOKEN, optional META_TEST_EVENT_CODE. If pixel ID or token is unset, silently no-op (mirror ghl.ts:26–27 behavior).
Endpoint: POST https://graph.facebook.com/v21.0/{META_PIXEL_ID}/events?access_token={token}. If META_TEST_EVENT_CODE is set, include test_event_code in the payload.
Use waitUntil from @vercel/functions (add the dependency): wrap the fetch promise in waitUntil() when available, guarded so local dev (where it may throw outside a Vercel request context) falls back to the dangling-fetch-with-swallowed-rejection pattern. Never throw to the caller under any circumstances.
Export sendMetaCapiEvent(params) accepting: eventName, eventId, email, phone (optional), firstName/lastName (optional), clientIp (optional), userAgent (optional), fbp (optional), fbc (optional), eventSourceUrl (optional), value/currency (optional), customData (optional object).
Hashing per Meta spec, using the same crypto.createHash("sha256") one-liner as server/lib/apiKeys.ts:18: email lowercased + trimmed before hashing; phone normalized to E.164 digits-only-with-country-code before hashing (add a small normalizePhoneE164 helper — assume US +1 default if 10 digits); first/last name lowercased + trimmed before hashing. client_ip_address, client_user_agent, fbp, fbc are sent UNHASHED per Meta spec.
Payload: action_source: "website", event_time as current unix seconds, event_id from params.

Part 2 — Call sites (mirror every existing isCompAccount / owner / first-transition gate exactly — fire CAPI immediately adjacent to each existing sendGhlEvent call, inside the same guard blocks)

Lead at all three partial_signup sites (replitAuth.ts:500, :567, :796). Include: email, firstName/lastName where available (OAuth paths), clientIp: req.ip, userAgent: req.headers["user-agent"], fbp: req.cookies._fbp ?? user.signupFbp, fbc: req.cookies._fbc ?? user.signupFbc. For /api/register: read metaEventId from the request body (see Part 3) and use it as eventId; OAuth paths generate a fresh UUID server-side.
StartTrial at trial_started (auth/routes.ts:159). Include: email, firstName, lastName, phone (E.164-normalized then hashed), clientIp, userAgent, live cookies with fallback to stored signupFbp/signupFbc. Read metaEventId from the PATCH body if present, else generate.
Activated (custom event) inside checkActivationMilestone (server/routes.ts:168), adjacent to the existing GHL call. Owner email only; fbp/fbc from owner row's signupFbp/signupFbc; no IP/UA. Generate eventId server-side.
Subscribe in sendGhlBillingEvent (stripeWebhook.ts) — fire ONLY for the checkout.session.completed / new-subscription path, NOT for updates or cancellations. Include owner email, value: mrr, currency: "USD", fbp/fbc from owner row. Generate eventId server-side. Confirm metaCapi.ts imports cleanly in the standalone webhook function context (server/vercelStripeWebhook.ts).

Part 3 — Client-side dedup plumbing

In register.tsx: generate crypto.randomUUID() before submitting; include it as metaEventId in the POST /api/register body; change the pixel call to fbq('track', 'Lead', {}, { eventID }) — replacing the current StartFreeSignup/CompleteRegistration calls (remove them; do not fire both old and new names).
In the welcome/profile-completion page: same pattern — UUID in PATCH body as metaEventId, pixel fires StartTrial with matching eventID.
Server: accept metaEventId as an optional string field in both route bodies, validate max length 64, ignore if absent.

Part 4 — Constraints

No schema changes, no migrations, no new tables.
Do not modify ghl.ts or any GHL behavior.
Do not touch the CSRF bypass list, session config, or attribution middleware.
TypeScript must compile with zero new errors — report the before/after error count.

Part 5 — Deliverable
Show the complete git diff, a list of files changed with line counts, and the TS error count before/after. Then STOP. No commits, no pushes.
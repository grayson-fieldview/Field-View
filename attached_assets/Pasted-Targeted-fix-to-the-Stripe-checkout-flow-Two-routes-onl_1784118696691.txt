Targeted fix to the Stripe checkout flow. Two routes only: /api/create-checkout-session and /api/confirm-checkout. Touch nothing else.Context: a real customer created a duplicate subscription by going through checkout while already subscribed. The current code has two flaws I've confirmed: (1) create-checkout-session detects an existing subscription (hasActiveSubscription check) but only uses it to skip trial_period_days — it still creates a new checkout session; (2) confirm-checkout calls subscriptions.list({ customer, limit: 1, status: "all" }) and blindly writes whichever subscription comes back first to stripeSubscriptionId.First, locate both routes in the actual source (use rg -n "create-checkout-session|confirm-checkout" server/) and confirm the current behavior matches the above. Then:Fix 1 — create-checkout-session: after loading the user, if the user (or their account row, if the account is source of truth) has a stripeSubscriptionId AND subscriptionStatus in (active, trialing, past_due), do NOT create a checkout session. Instead create a Stripe billing portal session for their stripeCustomerId (same pattern as the existing /api/create-portal-session route, return_url to /settings) and return its URL in the same { url } response shape, plus a field like { url, redirected: "billing_portal" }. The frontend already redirects to whatever url comes back, so this should require no client changes — verify that assumption by checking how the client consumes this response, and report if it needs a change.Fix 2 — confirm-checkout: replace the subscriptions.list({ limit: 1 }) logic. If the user already has a stored stripeSubscriptionId, retrieve THAT subscription by ID and sync its status; only fall back to listing when the stored value is null, and when listing, filter to statuses active/trialing (most recent by created) rather than taking the first of status: "all".Do not modify the webhook handler, the portal-session route, pricing routes, or anything else. Show me the diff before approving. Don't push.
Add two missing Stripe webhook handlers. Produce the diff, show it, stop.
No commit, no push.

Context: your event map confirmed auto-conversion is live — completing
checkout creates a real subscription that auto-charges when trial_end
passes, with no end_behavior and no cancel_at. We handle exactly three
webhook event types. Two gaps matter:

HANDLER 1 — customer.subscription.trial_will_end
Fires 3 days before trial_end. With auto-conversion live we currently
have NO hook to warn a user their card is about to be charged. That's
the surprise-charge / chargeback risk.

  - Slack alert: "⏰ Trial converting in 3 days: <email> — $X on <date>".
  - GHL: fire a billing_event so a workflow can send the heads-up email.
    Report what field name and payload shape matches the existing
    billing_event contract — don't invent a new one.
  - Idempotency: Stripe can redeliver. Confirm how the existing handlers
    guard against double-processing and use the same mechanism.

HANDLER 2 — invoice.payment_failed
Today a failed renewal only surfaces indirectly when Stripe flips the
sub to past_due and subscription.updated fires. No Slack alert at all.

  - Slack alert with email, amount, attempt count, and next retry date
    if Stripe provides it.
  - GHL billing_event so dunning can be automated later.
  - Do NOT change subscription status in the app — subscription.updated
    already owns that dual-write. This handler is notification only.
    Confirm there's no conflict.

FOR BOTH:
  - Register in the same if/else chain in handleSubscriptionEvent.
  - Both must be enabled in the Stripe dashboard webhook endpoint
    configuration or they'll never arrive. Tell me exactly which events
    to tick and where — I'll do it in Stripe after the deploy.
  - Confirm the signature verification path in vercelStripeWebhook.ts
    doesn't need changes for new event types.

ALSO REPORT (don't change):
  - What does invoice.paid currently do? Nothing per your map. Should
    renewals produce a Slack signal, or is that noise once there are
    real customers? Recommend one.
  - The Slack label on checkout.session.completed says "🆓 Trial signup"
    when amount_total === 0, which actually means "subscribed mid-trial,
    card on file, converts on <date>". Propose exact replacement copy for
    both branches of that heuristic. Don't change it yet — I want to see
    the wording first.
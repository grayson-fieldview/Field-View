node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment files — never commit real secrets.
# `.env.example` is intentionally tracked as the template.
.env
.env.local
.env.*.local